name: longread_postprocessing
channels:
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - python
  - samtools
  - ucsc-bedtobigbed
  - pip:
    - snakemake
    - jupyter
    - notebook
    - pytest
    - cython
    - seqkit
    - -e .

